import 'package:flutter/material.dart';

class PostCreator extends StatelessWidget {
  const PostCreator({Key? key}) : super(key: key);

  Widget postCreator() {
    return Text("Post creator function is work");
  }

  @override
  Widget build(BuildContext context) {
    return postCreator();
  }
}
